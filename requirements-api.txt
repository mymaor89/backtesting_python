# Runtime deps for the standalone backtest microservice (api_server.py).
# The replay engine itself needs psycopg2-binary + the editable shared package:
#   pip install -e /mnt/projects/algotrading/algotrading-strategies
fastapi>=0.110
uvicorn[standard]>=0.29
psycopg2-binary>=2.9
httpx>=0.27          # used by fastapi.testclient
